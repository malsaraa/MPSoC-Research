

# initiate all the tasks in task set into processors.
# This will assign maximum possible tasks to the processor 
# at the lowest speed rate possible using bin packing method. 

def taskInit(processorCount, speedrates, taskset):

    for each x in taskset
        for each y in proc
